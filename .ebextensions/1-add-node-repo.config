commands:
  add_node_repo:
    command: "curl -sL https://rpm.nodesource.com/setup_13.x | bash -"
  add_yarn_repo:
    command: "curl -sL https://dl.yarnpkg.com/rpm/yarn.repo | sudo tee /etc/yum.repos.d/yarn.repo"
